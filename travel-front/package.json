{
  "name": "travel-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite", // 开发环境启动（默认端口3000）
    "build": "vite build", // 生产环境打包（输出到dist目录）
    "preview": "vite preview" // 预览打包后的文件
  },
  "dependencies": {
    "axios": "^1.6.8", // API请求
    "pinia": "^2.1.7", // 状态管理
    "vue": "^3.4.21", // Vue 3核心
    "vue-router": "^4.3.0" // 路由
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4", // Vite Vue插件
    "vite": "^5.2.0" // Vite构建工具
  }
}